[project]
name = "shippear-juridico-agent"
version = "0.1.0"
description = "Proyecto de agentes jurídicos con LangChain y patrón supervisor/sub-agentes."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langchain",
    "langchain-core",
    "langgraph",
    "python-dotenv",
]
